import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#C6171E",
    },
    gradient1: {
      main: "#DB7702",
    },
    gradient2: {
      main: "#680795",
    },

    font1: { main: "#BBBBBB" },
  },
});
export default theme;
